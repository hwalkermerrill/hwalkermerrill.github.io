<%- include("../partials/header") %>

<!-- CONTENT HERE -->
<main class="clearfix" id="maincontent">
	<h1>Where Are We?!?</h1>
	<blockquote><strong>
			&nbsp;"The jungle knows no god but its own hunger. Mighty beasts enter, and not even bones return. Ancient
			rivers pour endlessly in, and are drunk down, every drop. Even spirits, given up beneath the canopy, become
			snared and tangled - dragged down amid roots and vines. But the jungle's hunger is not a dead thing like mine
			and yours. The jungle keeps what it eats, makes it part of itself, and grows rich and tall and ever more deadly.
			And amid its fat miles and heavy limbs what once were the ghosts of things that walk and crawl open new eyes
			amid the brush, and know a new hunger. A hunger like the jungle's."
		</strong></blockquote>
	<cite>&emsp; — Bahalos Voa, Mwangi Guide</cite>

	<!-- MAIN MAP PARTIAL -->
	<%- include("./_mainMap", { mainMap }) %>

	<!-- RETURN TO TOP BUTTON -->
	<button onclick="topFunction()" id="returnTopBtn" title="Go to top">Return to Top</button>
	<div class="clearfix"></div>
	<!-- RETURN TO TOP BUTTON -->

	<!-- GALLERY PARTIAL -->
	<h2>Maps of the Mwangi Expanse</h2>

	<!-- Admin Only Panel On-Click -->
	<% if (hasPermission(user, "manage_maps")) { %>
	<button type="button" onclick="toggleMapAdmin()">Manage Maps</button>

	<div id="map-admin-panel" style="display:none; margin-top:1rem;">
		<h3>Set Main Map</h3>

		<form method="POST" action="/maps/<%= mainMapId %>/set-main" id="set-main-map-form">
			<label for="main-map-select">Choose a map:</label>
			<select id="main-map-select" name="mapId">
				<% [...galleryTall, ...galleryWide].forEach(map => { %>
				<option value="<%= map.id %>" <%= map.id === mainMapId ? "selected" : "" %>>
					<%= map.item_name %>
				</option>
				<% }) %>
			</select>

			<button type="submit">Set as Main Map</button>
		</form>
	</div>
	<% } %>

	<%- include("./_gallery", { galleryTall, galleryWide }) %>
	<div class="clearfix"></div>

	<!-- LOCATIONS PARTIAL -->
	<%- include("./_locations", { currentLocation, pastLocations }) %>
	<div class="floatswitch"></div>
</main>

<script>
	function toggleMapAdmin() {
		const panel = document.getElementById("map-admin-panel");
		panel.style.display = panel.style.display === "none" ? "block" : "none";
	}

	// Update form action dynamically when dropdown changes
	document.addEventListener("DOMContentLoaded", () => {
		const select = document.getElementById("main-map-select");
		const form = document.getElementById("set-main-map-form");

		if (select && form) {
			select.addEventListener("change", () => {
				form.action = `/maps/${select.value}/set-main`;
			});
		}
	});
</script>

<%- include("../partials/footer") %>