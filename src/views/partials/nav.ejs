<nav class="site-nav">
	<div class="site-nav-group">
		<ul id="primaryNav" class="hide">
			<!-- Always Visible Links First -->
			<li><a href="/" aria-current="<%= activePage === 'home' ? 'page' : '' %>">
					<svg class="icon" aria-hidden="true" focusable="false">
						<use href="#icon-home"></use>
					</svg> Home </a></li>
			<li><a class="disabled-link" href="/" aria-current="<%= activePage === 'characters' ? 'page' : '' %>">
					Characters </a></li>
			<li><a class="disabled-link" href="/" aria-current="<%= activePage === 'assets' ? 'page' : '' %>"> Relics
				</a></li>
			<li><a href="/maps" aria-current="<%= activePage === 'maps' ? 'page' : '' %>">Maps</a></li>
			<li><a href="/journal" aria-current="<%= activePage === 'journal' ? 'page' : '' %>"> Journal
				</a></li>
			<li><a href="/rules" aria-current="<%= activePage === 'rules' ? 'page' : '' %>">Orders</a></li>
			<li><a href="https://hwalkermerrill.github.io">View Archives</a></li>

			<!-- Only Visible when NOT Logged In -->
			<% if (!isLoggedIn) { %>
			<li><a href="/login" aria-current="<%= activePage === 'login' ? 'page' : '' %>">Login</a></li>
			<li><a href="/register" aria-current="<%= activePage === 'register' ? 'page' : '' %>">Register</a></li>
			<% } %>
		</ul>
	</div>

	<!-- Only Visible when Logged In -->
	<% if (isLoggedIn) { %>
	<div class="site-nav-group">
		<ul id="secondaryNav" class="hide">
			<li><a href="/dashboard" aria-current="<%= activePage === 'dashboard' ? 'page' : '' %>">Dashboard</a></li>
			<li><a href="/register/account" aria-current="<%= activePage === 'register' ? 'page' : '' %>">Account</a></li>
			<!-- <li><a href="/contact/responses" aria-current="<%= activePage === 'contact' ? 'page' : '' %>">Contact Responses</a></li> -->
			<li><a class="disabled-link" href="/" aria-current="<%= activePage === 'creation' ? 'page' : '' %>">
					Character Builder </a></li>
			<li><a href="/logout" aria-current="<%= activePage === 'logout' ? 'page' : '' %>">Logout</a></li>
		</ul>
	</div>
	<% } %>
</nav>

<!-- FLASH MESSAGING HERE -->
<% 
  const messages = flash();
  const hasMessages = Object.values(messages).some(arr => arr.length > 0);
%>
<% if (hasMessages) { %>
<div class="flash-messages">
	<% Object.keys(messages).forEach(type => { %>
	<% messages[type].forEach(message => { %>
	<div class="flash <%= type %>">
		<%= message %>
	</div>
	<% }); %>
	<% }); %>
</div>
<% } %>